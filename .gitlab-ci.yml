stages:
  - test

backend-test:
  stage: test
  image: python:3.12-slim
  script:
    - cd api
    - pip install -r requirements-dev.txt
    - ruff check .
    - pytest -q

frontend-build:
  stage: test
  image: node:20-alpine
  script:
    - cd web
    - npm install
    - npm run build
